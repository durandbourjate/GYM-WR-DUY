# LearningView-Beschriebe: Vom Spielbuch zum Programm

> Kurzbeschriebe für die 14 Bausteine der Lerneinheit. Zum Einfügen in LearningView.

---

## Baustein 1: Kommentare

Du planst die Struktur deines Spiels, bevor du eine einzige Zeile Code schreibst. Mit Kommentaren (`#`) erstellst du ein Programmgerüst — wie ein Bauplan, der beschreibt, was später wo passieren soll.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 2: Ausgabe mit print()

Du lernst, wie Python Text auf dem Bildschirm ausgibt. Du schreibst die ersten Raumtexte für «Raus hier!» — mit mehrzeiligen Strings (`"""`), Trennlinien (`"=" * 50`) und Emojis.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 3: Variablen

Im Spielbuch musst du dir merken, in welchem Raum du bist und was du dabei hast. In Python übernehmen das **Variablen**. Du erstellst die Spielvariablen (Raum, Inventar, Fortschritt) und speicherst die Raumtexte in Variablen.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 4: Datentypen

Nicht jeder Wert ist gleich: der Tresorcode ist eine Zahl (`int`), ein Raumtext ist ein String (`str`), und ob du das Taschenmesser hast, ist `True` oder `False` (`bool`). Du lernst die vier wichtigsten Datentypen kennen und übst die Umwandlung zwischen ihnen.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 5: Eingabe mit input()

Jetzt wird dein Programm interaktiv: Der Spieler kann Eingaben machen. Du baust die Richtungswahl («osten/westen»), die Tresorcode-Eingabe und die Koffercode-Eingabe.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 6: Strings und f-Strings

Du machst deine Spieltexte dynamisch: Mit **f-Strings** setzt du Variablen direkt in den Text ein, z.B. den aktuellen Raumnamen oder das Inventar. Ausserdem lernst du, Spielereingaben mit `.lower().strip()` zu normalisieren.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 7: Operatoren

Du programmierst die Rätsellogik des Spiels: Das Sphinx-Rätsel (Tresorcode berechnen), das Zähl-Rätsel (Koffercode herausfinden) und die Wandelement-Auswahl für die Sprengung. Dabei verwendest du arithmetische, Vergleichs- und logische Operatoren.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 8: Verzweigungen (if / elif / else)

Jede Entscheidung im Spielbuch — «Willst du nach Osten oder Westen?» — wird zu einer Verzweigung im Code. Du baust die Startraum-Entscheidung, die Tresor-Interaktion und die Sprengungssequenz mit verschachtelten `if/elif/else`-Blöcken.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 9: Schleifen

Du baust das **Herzstück** des Spiels: die Spielschleife. Mit einer `while`-Schleife zeigt das Programm immer wieder den aktuellen Raum an, wartet auf deine Eingabe und wechselt zum nächsten Raum — bis du den Ausgang findest (oder aufgibst).

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 10: Listen

Statt für jeden Gegenstand eine eigene Variable zu führen, sammelst du alles in einer **Liste**. Du baust ein Inventarsystem, trackst besuchte Räume und integrierst beides in die Spielschleife.

**Vorgehen:** Lies zuerst die Theorie, dann bearbeite die Übung. Vergleiche erst danach mit der Musterlösung.

---

## Baustein 11: Zusammenfassung

Eine kompakte Übersicht: Welcher Python-Baustein entspricht welchem Element im Spielbuch? Diese Tabelle hilft dir beim Zusammensetzen des fertigen Spiels.

**Vorgehen:** Lies das Dokument durch und nutze es als Nachschlagewerk.

---

## Baustein 12: Tipps fürs Programmieren

Fünf praktische Tipps, die dir beim Programmieren helfen: Schritt für Schritt vorgehen, Fehlermeldungen lesen, häufige Fehler vermeiden, regelmässig testen und Kommentare schreiben.

**Vorgehen:** Lies das Dokument durch — besonders hilfreich, wenn du beim Programmieren nicht weiterkommst.

---

## Baustein 13: Erweiterungen

Du hast das Spiel mit den Grundkonzepten gebaut. Hier lernst du vier fortgeschrittene Konzepte kennen (Funktionen, Dictionaries, try/except, continue), mit denen die erweiterte Version eleganter und kürzer wird. Dieses Theorie-Dokument ist **optional** und für Schnelle gedacht.

**Vorgehen:** Lies das Dokument durch, wenn du das Grundspiel fertig hast und neugierig bist, wie man es verbessern kann.

---

## Baustein 14: Vom Spielbuch zum Programm

Warum braucht das Python-Spiel nur halb so viele Textblöcke wie das Spielbuch? Weil ein Programm **Variablen** hat und sich den Spielzustand merken kann — das Buch nicht. Dieses Dokument erklärt den zentralen Unterschied.

**Vorgehen:** Lies das Dokument durch, nachdem du das Spiel fertig programmiert hast. Es fasst zusammen, was du gelernt hast.
